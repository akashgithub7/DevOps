pipeline {
    agent any

    stages {
        stage('Dev') {
            steps {
                echo 'Ok'
                sh 'chmod +x script.sh'
                sh './script.sh'
            }
        }
    }
    post {
        always {
            mail bcc: '', body: 'Successfullt tested email notification via jenakins', cc: '', from: '', replyTo: '', subject: 'Java Program Run Successfully...', to: 'akashsangle377@gmail.com'
            
        }
    }
}
